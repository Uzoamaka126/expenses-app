import React from "react";
import { Wrapper, SubTitle, Title } from "./BoxWrapper";

export function StepThree(props) {
  return (
    <Wrapper>
      <Title heading="Create projects to do under a particular value." />
      <SubTitle text="After selecting a value, create as many projects as you would like under the selected value." />
    </Wrapper>
  );
}
